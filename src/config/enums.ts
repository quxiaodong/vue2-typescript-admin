export enum ResponseCode {
  success = 2000,
  unauthorized = 4010
}

export enum ElementSizes {
  mini = 'mini',
  small = 'small',
  medium = 'medium',
  large = 'large'
}

export enum Layouts {
  'side-menu-layout' = 'side-menu-layout',
  'top-menu-layout' = 'top-menu-layout',
  'mix-menu-layout' = 'mix-menu-layout'
}

export enum Locales {
  en = '🇺🇸 English',
  cn = '🇨🇳 简体中文'
}

export enum Schemes {
  light = 'light',
  dark = 'dark',
  'no-preference' = 'no-preference'
}

export enum ContentWidths {
  fixed = 'fixed',
  fluid = 'fluid'
}

export enum ClientSizes {
  xs = 'xs',
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl'
}
